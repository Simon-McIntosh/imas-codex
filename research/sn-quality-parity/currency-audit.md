# Standard Name documentation-quality claim currency

Audit target: `docs/sn-quality-parity.html` at tree `90c73937d264995fb3a12399c43c75346552004b` (plan storage version 9, read 2026-08-23).

Verdicts mean: **ALREADY-LANDED** — the claimed capability exists in the live tree; **STILL-OPEN** — the claim remains a coherent unimplemented deliverable; **SUPERSEDED** — the need is addressed through a materially different interface or lifecycle; **WRONG** — the live tree contradicts the claim. The rows cover every live-tree-testable technical assertion in the current-scope prose, §§1–4, and the still-operative followups/decisions. Dated census totals, cost estimates, past campaign outcomes, and workflow ownership statements in lines 80–138 are historical evidence or authority records, not claims about the current source tree, so they are not reclassified here.

| ID | Plan claim | Verdict | Live-tree evidence and disposition |
|---|---|---|---|
| C01 | Only unit persistence is resolved; implementation is 0.12 and all other engineering is open (`docs/sn-quality-parity.html:42-50`). | WRONG | The tree also has targeted focus routing (`imas_codex/cli/sn.py:1263-1277`), rich docs context (`imas_codex/standard_names/workers.py:8618-8641`), COCOS injection (`imas_codex/llm/prompts/sn/generate_docs_user.md:140-152`), content gates (`imas_codex/llm/prompts/sn/generate_docs_system.md:38-75`), and link lifecycle (`imas_codex/schemas/standard_name.yaml:1414-1429`). |
| C02 | Units persist both as scalar `unit` and `HAS_UNIT` (`docs/sn-quality-parity.html:49`). | ALREADY-LANDED | `StandardName.unit` has `range: Unit` and `HAS_UNIT` annotation (`imas_codex/schemas/standard_name.yaml:895-899`); writes set the scalar (`imas_codex/standard_names/graph_ops.py:5418-5428`) and self-heal the edge (`imas_codex/standard_names/graph_ops.py:5700-5729`). |
| C03 | A repeatable SN benchmark harness is needed (`docs/sn-quality-parity.html:50-56`). | ALREADY-LANDED | `sn bench` supports names plus docs (`imas_codex/cli/sn.py:3183-3187`) and a fixed 54-path run (`imas_codex/cli/sn.py:3291-3304`); the runner persists reproducible reports (`imas_codex/standard_names/benchmark.py:1446-1478`). |
| C04 | The required baseline is a documentation-quality holdout with catalog counterparts and gates for length, equations, signs, typical values, measurement, links, tags, validation, and reviewer score (`docs/sn-quality-parity.html:56`). | STILL-OPEN | The tracked benchmark is explicitly an SN naming benchmark and remains 20 positives + 20 negatives + 30 unfinished stubs (`tests/standard_names/eval_sets/benchmark.json:3-31`). Docs can be included, but no tracked holdout encodes the full claimed content-gate vector (`imas_codex/standard_names/benchmark.py:1317-1357`). |
| C05 | Every major phase must be compared with the baseline before claiming improvement (`docs/sn-quality-parity.html:35-36,56,68,89-90`). | STILL-OPEN | The only tracked phase-comparison set still has blank goldens (`tests/standard_names/eval_sets/prompt_ab.json:1-33`), and its harness declares itself scaffolded with compose wiring outstanding (`scripts/prompt_ab.py:1-27`). |
| C06 | A targeted `--paths` generator route must process exact DD paths outside the normal full-query/filter flow (`docs/sn-quality-parity.html:57`). | SUPERSEDED | The supported interface is `sn run --focus`, accepting repeated values or a manifest (`imas_codex/cli/sn.py:1263-1277`) and scoping the full pipeline by run UUID (`imas_codex/cli/sn.py:1724-1789,2287-2385`). Tests prove exact examples such as `equilibrium/time_slice/profiles_1d/psi` and `/q` are seeded (`tests/standard_names/test_focus_flag.py:117-151`). |
| C07 | `publish.py` should group output by `physics_domain` (`docs/sn-quality-parity.html:58`). | SUPERSEDED | Domain selection/grouping lives in the graph-to-staging half, `export.py`, which picks a primary domain and emits it after ISN validation (`imas_codex/standard_names/export.py:1527-1548`). `publish.py` is deliberately only staging-to-catalog transport (`imas_codex/standard_names/publish.py:1-14`). |
| C08 | Published entries should strip graph primary tags (`docs/sn-quality-parity.html:58`). | ALREADY-LANDED | The catalog projection emits name, descriptions, kind, unit, status and links but no `tags` (`imas_codex/standard_names/export.py:587-617`); domain routing is separate (`imas_codex/standard_names/export.py:1527-1548`). |
| C09 | A graph migration should remove primary tags from existing names (`docs/sn-quality-parity.html:58`). | WRONG | `tags` remain a live graph property used by name/docs queries (`imas_codex/standard_names/graph_ops.py:14597-14619,15283-15327`); the live design strips them only at the catalog projection boundary (`imas_codex/standard_names/export.py:587-617`). |
| C10 | A migration should prefix bare documentation links with `name:` (`docs/sn-quality-parity.html:58`). | SUPERSEDED | Accepted docs are normalized at promotion, linking live names and stripping dead/unknown bare brackets (`imas_codex/standard_names/graph_ops.py:15609-15627`); post-drain reconciliation remains a fallback (`imas_codex/standard_names/loop.py:2283-2304`). |
| C11 | Docs composition needs fuller DD path documentation (`docs/sn-quality-parity.html:59`). | ALREADY-LANDED | Docs generation queries linked `IMASNode` documentation/description (`imas_codex/standard_names/workers.py:8499-8512`) and resolves rich-first DD context into the prompt item (`imas_codex/standard_names/workers.py:8663-8720`). |
| C12 | Docs composition needs COCOS behavior from DD authority (`docs/sn-quality-parity.html:59`). | ALREADY-LANDED | The docs query returns `cocos_transformation_type`; enrichment renders authoritative guidance (`imas_codex/standard_names/workers.py:8499-8504,8789-8801`). |
| C13 | Compose needs coordinate context (`docs/sn-quality-parity.html:59`). | ALREADY-LANDED | The common enrichment layer explicitly fetches coordinate specifications with COCOS, identifiers, siblings, and related paths (`imas_codex/standard_names/workers.py:2106-2152`); docs additionally inherit source/ancestor context (`imas_codex/standard_names/workers.py:8725-8749`). |
| C14 | Compose needs related-path context (`docs/sn-quality-parity.html:59`). | ALREADY-LANDED | Docs enrichment derives graph-relationship neighbours and DD path context from the first source path (`imas_codex/standard_names/workers.py:8809-8829`), rendered as related physics quantities (`imas_codex/llm/prompts/sn/generate_docs_user.md:254-260`). |
| C15 | Compose needs structure/ancestor context (`docs/sn-quality-parity.html:59`). | ALREADY-LANDED | The enrichment walks rich ancestors and supplies up to four nearest lineage entries (`imas_codex/standard_names/workers.py:8725-8749`); the prompt also renders parent structure (`imas_codex/llm/prompts/sn/generate_docs_user.md:282-286`). |
| C16 | DD enrichment must use the shared library/query layer, not MCP-formatted chat output (`docs/sn-quality-parity.html:59,73,90`). | ALREADY-LANDED | Workers import and use `GraphClient` directly for enrichment (`imas_codex/standard_names/workers.py:2146-2152,8618-8650`); the prompt receives structured dictionaries rather than MCP prose (`imas_codex/standard_names/workers.py:8621-8638`). |
| C17 | COCOS-dependent docs must contain an explicit standalone sign-convention paragraph (`docs/sn-quality-parity.html:60,65,90`). | ALREADY-LANDED | The system template mandates exact `Sign convention: Positive when ...` form (`imas_codex/llm/prompts/sn/generate_docs_system.md:45-59`), and the per-item COCOS block repeats it (`imas_codex/llm/prompts/sn/generate_docs_user.md:140-152`). |
| C18 | Docs require a self-contained definition (`docs/sn-quality-parity.html:32-34,65`). | ALREADY-LANDED | Definition is template item 1 (`imas_codex/llm/prompts/sn/generate_docs_system.md:38-43`) and the output contract requires a self-contained first sentence (`imas_codex/llm/prompts/sn/generate_docs_system.md:229-231`). |
| C19 | Docs require a governing equation and variable definitions when applicable (`docs/sn-quality-parity.html:33,65`). | ALREADY-LANDED | Template item 2 requires the principal display equation with variables defined in prose, and the quality checklist enforces exactly one principal display equation where applicable (`imas_codex/llm/prompts/sn/generate_docs_system.md:42-43,201-206`). |
| C20 | Docs require a distinct significance section (`docs/sn-quality-parity.html:33,65`). | SUPERSEDED | The live normative template instead requires semantic scope/exclusions and essential relationships (`imas_codex/llm/prompts/sn/generate_docs_system.md:42-45,229-231`); it has no mandatory standalone significance section. |
| C21 | Docs require a general measurement/calculation section (`docs/sn-quality-parity.html:33-34,65`). | WRONG | The live faithfulness policy forbids generic diagnostic/method material and allows measurement/computation only when constitutive or discriminating (`imas_codex/llm/prompts/sn/generate_docs_system.md:172-182`; `imas_codex/llm/prompts/sn/generate_docs_user.md:326-333`). |
| C22 | Docs require typical values (`docs/sn-quality-parity.html:34,56,65,90`). | WRONG | Typical values, experiment ranges, and device examples are explicitly forbidden to prevent plausible but ungrounded filler (`imas_codex/llm/prompts/sn/generate_docs_system.md:176-182,229-232`; `imas_codex/llm/prompts/sn/generate_docs_user.md:326-333`). |
| C23 | Docs require essential working cross-references (`docs/sn-quality-parity.html:34-35,65`). | ALREADY-LANDED | The template requires inline `name:` links and no trailing “See also” block (`imas_codex/llm/prompts/sn/generate_docs_system.md:75,201-206`); authoritative candidates are injected (`imas_codex/llm/prompts/sn/generate_docs_user.md:245-301`). |
| C24 | Content gates matter more than a hard minimum character target (`docs/sn-quality-parity.html:65`). | ALREADY-LANDED | The old 200–500 character target is gone: docs target 40–160 words with hard 20–250-word bounds (`imas_codex/llm/prompts/sn/generate_docs_system.md:193-199`), while the schema requires substantive physics/equation/scope/relationship/sign content (`imas_codex/standard_names/models.py:1812-1847`). |
| C25 | Prompting needs a small set of complete curated examples (`docs/sn-quality-parity.html:66,90`). | ALREADY-LANDED | A versioned curated snapshot contains full description/documentation/equation/link examples such as `internal_inductance`, `effective_charge`, and `plasma_current` (`imas_codex/standard_names/examples_curated.yaml:5-53,118-136`); context loads the strongest tiers for prompt injection (`imas_codex/standard_names/context.py:338-368`). |
| C26 | Prompting also needs broad vocabulary coverage beyond the showcase examples (`docs/sn-quality-parity.html:66`). | ALREADY-LANDED | The context layer injects every closed-vocabulary segment and its full token list (`imas_codex/standard_names/context.py:193-220`), while compose also loads graph-scored examples by domain and score bucket (`imas_codex/standard_names/example_loader.py:229-305`). |
| C27 | Curated examples must be provably disjoint from the holdout paths (`docs/sn-quality-parity.html:66,90`). | STILL-OPEN | The curated snapshot carries names/docs but no DD-path split key (`imas_codex/standard_names/examples_curated.yaml:5-25`), and the tracked benchmark has no exclusion-manifest or overlap assertion (`tests/standard_names/eval_sets/benchmark.json:1-40`). |
| C28 | Link lifecycle needs `link_status`, `link_checked_at`, and `link_retry_count` (`docs/sn-quality-parity.html:67`). | ALREADY-LANDED | All three fields and their resolved/unresolved/failed semantics are schema-owned (`imas_codex/schemas/standard_name.yaml:1414-1429`). |
| C29 | Link lifecycle needs a separate `unresolved_links` schema field (`docs/sn-quality-parity.html:67`). | WRONG | No such schema field exists; unresolvedness is encoded by `dd:` entries in the canonical `links` list plus `link_status` (`imas_codex/standard_names/graph_ops.py:378-388`; `imas_codex/schemas/standard_name.yaml:1414-1429`). |
| C30 | Compose should emit plausible `name:` links beyond the known-name list (`docs/sn-quality-parity.html:67`). | WRONG | The live prompt says the injected names are the only permissible internal targets and forbids guessed IDs because they fail validation (`imas_codex/llm/prompts/sn/generate_docs_user.md:288-301`); the system contract likewise requires existing targets (`imas_codex/llm/prompts/sn/generate_docs_system.md:206,231-232`). |
| C31 | Link resolution should be a separate public `sn resolve-links` worker command (`docs/sn-quality-parity.html:67`). | SUPERSEDED | Public `sn resolve-links` is intentionally absent and rejected (`tests/standard_names/test_sn_help_no_reconcile.py:31-39,55-59`); resolution runs as internal post-drain maintenance (`imas_codex/standard_names/loop.py:2283-2304`). |
| C32 | Link resolution needs bounded retries and a terminal `failed` state (`docs/sn-quality-parity.html:67,74,90`). | ALREADY-LANDED | Claims exclude attempt-capped rows (`imas_codex/standard_names/graph_ops.py:8673-8705`); the fifth unresolved attempt writes `failed` and persists check time/count (`imas_codex/standard_names/graph_ops.py:8793-8818`). |
| C33 | Unresolved internal links must not reach published output (`docs/sn-quality-parity.html:67,75,90`). | ALREADY-LANDED | Final export blocks dangling links while RC reports advisories (`imas_codex/standard_names/export.py:386-412`), and the emitted catalog prunes any internal target absent from the final published set before write (`imas_codex/standard_names/export.py:939-969,1593-1598,1649-1659`). |
| C34 | A/B validation must rerun the holdout after each major phase (`docs/sn-quality-parity.html:68,90`). | STILL-OPEN | The 20-path A/B/C set has empty `golden_name` fields (`tests/standard_names/eval_sets/prompt_ab.json:1-33`); the harness says concrete compose-worker wiring remains outstanding (`scripts/prompt_ab.py:18-27`). |
| C35 | Final validation needs a bulk-generation sample such as equilibrium against catalog baselines (`docs/sn-quality-parity.html:68`). | STILL-OPEN | The repeatable benchmark is a fixed 54-path run (`imas_codex/cli/sn.py:3291-3304`), while the tracked A/B scaffold only defines 20 paths and no bulk equilibrium result (`tests/standard_names/eval_sets/prompt_ab.json:1-33`). |
| C36 | Link resolution must clean superseded/dead targets, not only initial `dd:` placeholders (`docs/sn-quality-parity.html:51,67,74`). | ALREADY-LANDED | `resolve_doc_links` rewrites superseded targets to accepted successors or removes dead-end hyperlinks, then refreshes cached links/status (`imas_codex/standard_names/graph_ops.py:8986-8998,9117-9137`). |
| C37 | This plan still feeds “Plan 34” and state-of-the-nation research (`docs/sn-quality-parity.html:76`). | WRONG | Live plan metadata says it informs only `sn-west-review-rehearsal` (`docs/sn-quality-parity.html:16`); the surviving benchmark relationship points the other way—`sn-benchmark-evolution` informs this plan (`docs/sn-benchmark-evolution.html:17,59`). No state-of-the-nation resource is present in the live docs/research tree. |
| C38 | AGENTS guidance remains to be updated when this work lands (`docs/sn-quality-parity.html:77`). | WRONG | Current repo guidance already documents seven-pool docs generation/review/refine, COCOS injection, link hygiene, family harmonization, and publish behavior (`AGENTS.md:579-671`). |
| C39 | A research quality-gap analysis remains to be moved (`docs/sn-quality-parity.html:77`). | SUPERSEDED | No quality-gap research artifact exists in the live `research/` or `docs/` inventory; the maintained authority is the HTML plan plus benchmark resources (`docs/sn-quality-parity.html:29-78`; `tests/standard_names/eval_sets/benchmark.json:1-31`). |
| C40 | Schema references remain to be updated (`docs/sn-quality-parity.html:77`). | SUPERSEDED | Link lifecycle and unit authority are already represented in the LinkML source (`imas_codex/schemas/standard_name.yaml:868-899,1414-1429`); the repository treats `agents/schema-reference.md` as generated output rather than a hand-maintained implementation target (`AGENTS.md:190-199`). |
| C41 | `publish.py` docstrings still need to move with the work (`docs/sn-quality-parity.html:77`). | WRONG | Its module docstring already states the current two-step boundary and safety contract, including staged-domain checks and post-copy validation (`imas_codex/standard_names/publish.py:1-14`). |
| C42 | Improved docs regeneration is scoped to WEST plus individually proven failures; no graph-wide rewrite (`docs/sn-quality-parity.html:35-37,120-127`). | ALREADY-LANDED | The CLI has exact focus/manifest scoping (`imas_codex/cli/sn.py:1263-1288`) and a budgeted documentation campaign route (`imas_codex/cli/sn.py:2506-2510`); no graph-wide regeneration mechanism is required for the policy to operate. |
| C43 | Documentation generation/review/refine use paid, separately configured seats rather than inheriting the free compose seat (`docs/sn-quality-parity.html:137-138`). | ALREADY-LANDED | Separate production sections exist for `[sn-docs]`, `[sn-refine]`, and docs review (`pyproject.toml:297-305,621-629`), distinct from `[sn-compose]` (`pyproject.toml:380-388`). This also supersedes the older “docs-pending improves for free” assumption at `docs/sn-quality-parity.html:122`. |

## Headline disposition

43 live-tree technical claims were classified: **22 ALREADY-LANDED, 5 STILL-OPEN, 7 SUPERSEDED, and 9 WRONG**. The open core is narrower than the plan says: a documentation-specific, disjoint holdout; phase-by-phase evidence; and a bulk validation run. Rich DD/COCOS context, the normative content template, scoped generation, link lifecycle, and publish filtering are already present, while “typical values,” general measurement recipes, guessed links, and a graph `unresolved_links` field contradict the current design.
