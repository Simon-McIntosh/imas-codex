# Drain ledger — imas-codex S10, session ship-s10-20260904

Written before the closing report, per the followup-drain rule. Every open queue row
carries a disposition from the closed set. A row marked `folded` names the node that
took it; a row left open names the authority, decision, owner or figure that keeps it open.

## Rows folded, with the node that took them

| Row | Node |
|---|---|
| `f-sli-001` close every open write path | `n-sli-the-import-cannot-write-the-editorial-axis`, `n-sli-fit-artifact-means-what-it-says`, `n-sli-eligibility-fixtures-carry-a-category`, `n-sli-a-rebuild-honours-an-active-resolution` |
| `f-swct-001` batch scoping (§1 half) | `n-swct-a-cut-publishes-only-its-own-manifest` |
| `f-crs-002` exercise the fold-back rehearsal | `n-crs-can-the-rehearsal-be-aimed-today`, `n-crs-graph-pull-names-its-target`, `n-crs-currency-distinguishes-a-recovery-archive` |
| ISN measurement-direction gap (`sn-release-readiness` §12) | `n-sgr-four-vocabulary-gaps` — resolved as a **rejection**; the shipped ordinal pair already binds all six leaves |
| export report does not gate publication | `n-wcr-a-failed-export-stops-publication` |
| exact terminal-identity release cohort (reuse-map follow-on) | `n-swct-a-cut-publishes-only-its-own-manifest` |
| consumer tests pin pre-editable assumptions | `n-sgr-consumer-tests-read-the-grammar-they-resolve` |
| merged-head regression, 27 added failures | `n-sli-eligibility-fixtures-carry-a-category` (19), `n-sgr-consumer-assertions-read-the-merged-grammar` (7), `n-sli-which-unit-authority-is-right` (1) |
| live-graph unit disagreement | `n-sli-does-a-rebuild-bypass-a-resolution` → `n-sli-a-rebuild-honours-an-active-resolution` |
| `power_flux_density` fifth vocabulary gap | `n-sgr-the-fifth-vocabulary-gap` |

## Rows left open, with the reason each stays open

| Row | Disposition | The authority, owner or figure |
|---|---|---|
| retire `mint_sn_list` or keep it as an extension point | `authority-required` | a design judgement on intent: the function has no production caller but its family closure was deliberate and its tests still cover it |
| four per-solid-angle emission-rate renames | `authority-required` | changes **published names** through a review quorum and spends review budget; two of the four need rescoring first because their rotation cap is spent |
| tag and publish the ISN release to PyPI | `authority-required` | an outward-facing publish under the lead's account |
| six `sensor/direction` leaves report unit `m` despite unit-vector semantics | `foreign-owner` | a Data Dictionary defect; needs a DD-owning plan |
| reckon peer channel reports no peers while peers are live | `foreign-owner` | reckon; cost one round trip per scope extension, observed twice |
| spend hold keyed per project on a per-account resource | `foreign-owner` | reckon |
| 31 born-ungrounded rows | `foreign-owner` | `sn-regeneration-decision` (S12) |
| cohort Groups A, B and E | `context-exhausted` | codex weekly window at **84%** against a 95% ceiling, resetting 2026-09-11; these need operational pipeline runs |
| rebuild the 72 never-grounded chain-cap identities | `context-exhausted` | same figure |
| divergence scan walks the catalog `.venv`; fallback PR body omits `REVIEWING.md`; stale domain reclassifier and `gh` CLI calls; rename cascade planner prints work it never performs | `context-exhausted` | same figure; all four are non-blocking release residues |
| optional-dependency coverage gap: 6 files, 54 declared test functions | `context-exhausted` | same figure; measured and recorded, not blocking |

## Run reconciliation

`crew drain` reports **live_pointers 0, unreconciled_runs 0**. All 19 worktrees this
session created were removed after their commits were confirmed reachable.

Seven worktrees from **earlier** sessions are dirty or unintegrated and were deliberately
not reclaimed: five under `ship-s10-20260901`, one under `s12hyg-20260825a`, one under
`west-demo-20260901d`. They carry uncommitted content this session did not author.
